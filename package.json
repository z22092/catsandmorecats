{
  "name": "cats-express",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "export $(cat .env | xargs) && node --harmony-top-level-await ./bin/www",
    "dev": "export $(cat .env | xargs) && nodemon  node --harmony-top-level-await ./bin/www"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "^4.2.0",
    "express": "^4.17.1",
    "http-errors": "^1.8.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.5"
  }
}
